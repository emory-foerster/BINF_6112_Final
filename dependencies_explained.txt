name: binf_final
channels:
  - bioconda
  - defaults
dependencies:
  - biopython
    #help with open reading frame, and reading FASTA files
  - matplotlib
    # visualize frameshift results, ORF lengths and dominance matrix
prefix: /opt/miniconda3/envs/binf_final
